if F:
	print("true");
	if T:
		print("inner if is true");
	else:
		print("inner if is false");
	endif;
else:
	print("false");
	if "this isn't a boolean":
		print("inner if is true");
	else:
		print("inner if is false");
	endif;
endif;